# Copie este arquivo para .env e ajuste os valores conforme necessario.
# NUNCA versione o arquivo .env real.

# Porta em que o servidor Flask sera iniciado
PORT=5000

# 1 = modo debug (recarrega automaticamente), 0 = modo normal
FLASK_DEBUG=1
